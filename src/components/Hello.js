import React from "react";

let divStyle = {
  backgroundColor: "mintcream"
};

const Hello = () => (
  <div style={divStyle}>
    <h1>Hello, world.</h1>
  </div>
);

export default Hello;
